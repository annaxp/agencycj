mixin background(videos)
  .background
    video.background__video(loop='loop' autoplay='autoplay' muted='muted' playsinline='playsinline')
      each source in videos
        source(src=source.src, type=source.type)
