//- require mixins block
include ~@mixins/service-icon

mixin services-list(props)
	.services-list
		each service in props.services
			- var elementTag = service.hasLink ? 'a' : 'div'
			- var title = service.name
			- var desc = service.preview_text
			- var icon = service.icon_name
			- var href = 'services/'+service.code+'.html'
			.services-item
				.services-item__link-wrapper
					#{elementTag}.services-item__link(href=href)
						.services-item__icon
							+service-icon(icon)
						.services-item__title= title
						.services-item__desc= desc
					a.services-item__hover(href="#contacts")
						include ~@assets/icons/services/order.svg
			